# Guestbook Page Frame

The Guestbook page now uses the standard passenger page frame:

```txt
PAGE_FRAME_CLASS
```

Both Guestbook columns fit within the usable tablet/kiosk height:

```txt
Leave a note card
Ride Wall card
```

The headings stay fixed inside each card, and longer form/wall content scrolls internally instead of pushing the full passenger screen.
