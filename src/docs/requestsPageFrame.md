# Requests Page Frame

The passenger Requests page now uses the standard page frame:

```txt
PAGE_FRAME_CLASS
```

The main card fits the available tablet/kiosk height. The title/status header stays fixed inside the card, while request status cards and request category buttons scroll inside the card when needed.

The request selection modal remains full-screen because it is a temporary focused action layer.
